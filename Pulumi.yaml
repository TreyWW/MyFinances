name: myfinances
main: /infrastructure/aws/pulumi/
stackConfigDir: /infrastructure/aws/pulumi/
runtime:
  name: python
  options:
    virtualenv: venv
description: MyFinances infrastructure
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
